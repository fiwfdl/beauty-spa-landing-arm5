# Serenity Spa — landing (bench arm 5)

Static Next.js marketing landing for Serenity Spa. `output: 'export'` → Cloudflare Pages.

## Develop

- `npm install`, `npm run dev`
- `npm test`, `npm run build`
